{
  "name": "cubecraft-leaderboard-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dashboard:build": "bun run --cwd dashboard build",
    "dashboard:dev": "bun run --cwd dashboard dev",
    "api:dev": "bun --hot api/src/index.ts",
    "api:migrate": "bun api/src/migrate.ts",
    "api:bench": "bun api/src/bench.ts",
    "db:refresh": "bun scripts/refresh-db.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}